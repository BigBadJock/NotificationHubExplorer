<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NotificationHubExplorer.App.Views.InstallationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NotificationHubExplorer.App.Views"
    xmlns:models="using:NotificationHubExplorer.Core.Models">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Padding="16,8">
            <Button Content="Load Installations" Command="{x:Bind ViewModel.LoadInstallationsCommand}"
                    IsEnabled="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}"/>
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" Width="20" Height="20"/>
        </StackPanel>
        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Installations, Mode=OneWay}" Padding="16,0">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:InstallationSummary">
                    <StackPanel Spacing="4" Padding="0,8">
                        <TextBlock Text="{x:Bind InstallationId}" FontWeight="SemiBold"/>
                        <TextBlock Text="{x:Bind Platform}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock Grid.Row="2" Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Padding="16,8"/>
    </Grid>
</Page>
